<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/detail_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/kolsa_background">

    <!-- Название приложения -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="FitnessApp"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/kolsa_pink"
        android:layout_marginBottom="12dp"/>

    <!-- Название тренировки -->
    <TextView
        android:id="@+id/workoutTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Название упражнения"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/kolsa_text_primary"
        android:layout_marginBottom="8dp"/>

    <!-- Видео плеер -->
    <com.google.android.exoplayer2.ui.StyledPlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="@color/kolsa_teal"
        app:use_controller="true"
        app:show_buffering="when_playing"
        app:resize_mode="fit"
        android:keepScreenOn="true"
        android:layout_marginBottom="8dp"/>

    <!-- Длительность видео -->
    <TextView
        android:id="@+id/videoDuration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Длительность: 30 минут"
        android:textColor="@color/kolsa_text_secondary"
        android:layout_marginBottom="8dp"/>

    <!-- Описание упражнения -->
    <TextView
        android:id="@+id/workoutDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Описание упражнения"
        android:textSize="16sp"
        android:textColor="@color/kolsa_text_primary"
        android:layout_marginBottom="8dp"/>

    <!-- Прогрессбар -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_marginTop="12dp"
        android:indeterminateTint="@color/kolsa_pink"/>

    <!-- Текст ошибки -->
    <TextView
        android:id="@+id/errorText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/holo_red_dark"
        android:visibility="gone"
        android:text="Ошибка загрузки видео"
        android:layout_marginTop="8dp"/>
</LinearLayout>
